config:
    url: "https://"
    fields:
        id:
            type: int
            key: id
        title:
            type: string
            key: name
        abilities:
            type: array
            key: permissions
        created_at:
            type: date
            key: generated_at
